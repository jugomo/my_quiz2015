<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>
      Quiz
    </title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h2>Quiz: el juego de las preguntas</h2>

    <% if (respuesta == "Correcto") { %>
      <font color="#77FF77">
    <% } else if (respuesta == "Incorrexto") { %>
      <font color="red">
    <% } %>
    <%= respuesta %></font>

    <p>
    <a href='/quizes/question'>Intentelo otra vez</a>
  </body>
</html>
